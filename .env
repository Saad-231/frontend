# Used only if you deploy frontend/backend separately (not localhost dev).
# In local dev, the Vite proxy (vite.config.js) handles /api and /socket.io.
VITE_API_BASE_URL=http://localhost:5000
VITE_SOCKET_URL=http://localhost:5000
